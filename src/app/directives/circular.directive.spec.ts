import { CircularDirective } from './circular.directive';

describe('CircularDirective', () => {
  it('should create an instance', () => {
    const directive = new CircularDirective();
    expect(directive).toBeTruthy();
  });
});
